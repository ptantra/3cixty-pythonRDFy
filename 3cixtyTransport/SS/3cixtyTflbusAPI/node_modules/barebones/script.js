#!/usr/bin/env node
var gitdown = require('github-download');
var colors = require('colors');

var directory = process.argv[2] || "barebones";
console.log("installing " + directory.yellow.bold);

module.exports = gitdown("https://github.com/dsiah/Barebones.git", "./" + directory);